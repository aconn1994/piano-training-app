// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC_-dj73YBmh43V2E1Rl1MTdAC-6QVAFws",
    authDomain: "piano-training-app.firebaseapp.com",
    projectId: "piano-training-app",
    storageBucket: "piano-training-app.appspot.com",
    messagingSenderId: "512253664087",
    appId: "1:512253664087:web:639a7fd417261c51937c12",
    measurementId: "G-CCVN7GDN10"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
